# WorldMonitor (separate app). Sidebar "World news" opens this URL in a new tab.
# Copy to .env.local and adjust port if your WorldMonitor dev server differs.
VITE_WORLD_MONITOR_URL=http://localhost:3000
