/**
 * Fx Live News localStorage keys (kept separate from WorldMonitor `STORAGE_KEYS` in variants/base).
 */
export const STORAGE_KEYS = {
  liveChannels: 'fx-live-news-channels',
  activeChannel: 'fx-live-news-active-channel',
} as const;
